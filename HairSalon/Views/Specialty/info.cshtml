@using HairSalon.Models;
<h1>Bangs</h1>
<h2>Specialty: @Model["thisSpecialty"].GetDescription()</h2>
@if (Model["stylists"].Count != 0)
  {
    <ul>
      @foreach(var stylist in Model["stylists"])
      {
        <li><a href="/items/@item.GetId()">@stylist.GetName()</a></li>
      }
    </ul>
<form action='/client/@Model["specialtyId"]/info' method="post">
  <select class="newStylist" name="newStylist">
    @foreach (var stylist in Model["stylists"])
    {
      <option value="@stylist.GetName()">@stylist.GetName()</option>
    }
  </select>
  <button type="submit">Update Client Name</button>
</form>
<h4><a href="/client/Index">Return to Client List</a></h4>
